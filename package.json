{
  "name": "browserify",
  "version": "1.0.0",
  "description": "desc",
  "main": "index.js",
  "scripts": {
    "build": "browserify -r ./main.js:app > app.js",
    "serve": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nmr",
  "license": "ISC",
  "dependencies": {
    "hyperscript": "^2.0.2"
  },
  "devDependencies": {}
}
